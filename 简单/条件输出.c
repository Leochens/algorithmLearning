/*
编程实现输出1—100之间能被7整除但不能被3整除的所有偶数，
并求满足该条件的所有数的累加和。
**要求输入提示信息为：无输入提示信息和输入数据
**要求输出格式为：（1）"%5d"  
                  （2）"\nsum=%d\n"

 */


#include <stdio.h>

main()
{
    int i,sum=0;
    for(i=1;i<100;i++)
    {
        if(i%7==0&&i%3!=0&&i%2==0)
        {
            printf("%5d",i);
           sum+=i;
        }
    }
    printf("\nsum=%d\n",sum);
    
}